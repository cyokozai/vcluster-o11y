# Pattern B: OTel Collector なし (OTel SDK → Alloy 直接)
# vcluster create vcluster-2 -n vcluster-2 --values manifests/vcluster/vcluster-2-config.yaml
controlPlane:
  distro:
    k3s:
      enabled: true
      image:
        tag: v1.31.4-k3s1

  backingStore:
    etcd:
      deploy:
        enabled: false

  statefulSet:
    resources:
      limits:
        cpu: "4"
        memory: "4Gi"
      requests:
        cpu: "500m"
        memory: "512Mi"

sync:
  toHost:
    services:
      enabled: true
    persistentVolumeClaims:
      enabled: true
  fromHost:
    nodes:
      enabled: true
      selector:
        all: true
    storageClasses:
      enabled: true

# Pattern A/B: fromHost で Alloy を仮想クラスタ内に複製
networking:
  replicateServices:
    fromHost:
      - from: monitoring/alloy
        to: default/alloy

policies:
  podSecurityStandard: baseline
  resourceQuota:
    enabled: true
    quota:
      count/pods: "20"
      count/services: "20"
  limitRange:
    enabled: true
    default:
      cpu: "500m"
      memory: "512Mi"
    defaultRequest:
      cpu: "100m"
      memory: "128Mi"
