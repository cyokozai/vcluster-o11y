# Pattern A: Go API Server (OTel SDK + OTel Collector)
# kubectl apply -f manifests/pattern-a/otelcol.yaml
# kubectl apply -f manifests/pattern-a/deploy.yaml
#
# テレメトリフロー: go-api-server → OTel SDK → otelcol:4317 → alloy:4317 (replicateServices) → ホスト Alloy
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: go-api-server
  namespace: default
spec:
  replicas: 1
  selector:
    matchLabels:
      app: go-api-server
      pattern: "a"
  template:
    metadata:
      labels:
        app: go-api-server
        pattern: "a"
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        runAsGroup: 65532
        fsGroup: 65532
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: go-api-server
          image: <ECR_REPOSITORY_URL>:latest
          ports:
            - containerPort: 8080
          env:
            - name: PORT
              value: "8080"
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "otelcol.default.svc.cluster.local:4317"
            - name: OTEL_SERVICE_NAME
              value: "go-api-server-pattern-a"
            - name: OTEL_EXPORTER_OTLP_PROTOCOL
              value: "grpc"
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: go-api-server
  namespace: default
spec:
  selector:
    app: go-api-server
    pattern: "a"
  ports:
    - port: 8080
      targetPort: 8080
